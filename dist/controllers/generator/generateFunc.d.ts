export declare function generateFunc(vecLength: number, side: number, exp: number, bias: number, noise: string | number, weightTerrain: number[], weightGridType: number[]): {
    terrain: number[][];
    gridType: number[][];
};
